# New-API 生产环境配置
# 复制此文件为 .env 并修改以下配置

# 应用版本（自动部署时会更新）
VERSION=latest

# Session 密钥（必须修改！用于加密 session）
# 生成方法：openssl rand -hex 32
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_STRING

# Redis 密码（必须修改！）
# 生成方法：openssl rand -base64 32
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD
